node_modules/
dist/
.wrangler/
.dev.vars
.env
.env.*
*.local
.DS_Store

# keep example templates tracked
!.dev.vars.example
!.env.example

# design handoff drop (kept out of the repo)
.design-handoff/
FGC-Log-Frontend.zip

# Notebook Prep publish secret, read by the runbook at publish time (never committed)
.notebook-secret
